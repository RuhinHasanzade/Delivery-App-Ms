spring:
  application:
    name: orderms

  datasource:
    url: jdbc:mysql://localhost:3306/order_db
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8085

services:
  menu:
    url: http://localhost:8082/menu-service-ms
  user:
    url: http://localhost:8080
  notification:
    url: http://localhost:8088/notificationservice-ms